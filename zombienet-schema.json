{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ZombieNet DSL",
  "description": "JSON Schema definition for ZombieNet DSL Schema",
  "type": "object",
  "properties": {
    "Description": {
      "title": "Description of the network",
      "$ref": "#/$defs/Description",
      "minLength": 3,
      "maxLength": 64,
      "markdownDescription": "The description of the network. It is a string from 3..64 chars."
    },
    "Network": {
      "title": "Name of the network file",
      "$ref": "#/$defs/file",
      "markdownDescription": "The name of the network file. Should be a toml or a json file"
    },
    "Creds": {
      "title": "Creds configuration something",
      "type": "string",
      "enum": ["config", "foo", "bar"],
      "markdownDescription": "----------"
    }
  },
  "$defs": {
    "Description": { "type": "string" },
    "file": { "type": "string", "pattern": ".*\\.(toml|json)$" },
    "statement": { "type": "string" }
  },
  "required": [
    "Description",
    "Network",
    "Creds"
  ]
}
